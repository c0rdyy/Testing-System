array[] int;
matrix[][] int;
Используя круглые скобки `()`
matrix[row.column]
array.size
matrix.columns
`()`
Нет необходимости указывать тип данных
Передача массива с использованием `malloc()`
Нет, это возможно только при динамическом выделении памяти.
Одномерный массив может быть проинициализирован с помощью оператора `new`, в то время как двумерный массив - нет.